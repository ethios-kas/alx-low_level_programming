#include <stdio.h>
/**
 * main - Start of the program
 * Return: success bring me the money
 */

int main(void)
{
	int n, a;

	for (n = '0'; n <= '9'; n++)
	putchar(n);

	for (a = 'a'; a <= 'f'; a++)
	putchar(a);
	putchar('\n');
	return (0);
}
