#include "main.h"

/**
 * _strchr - locate a character within a string
 * @s: pointer to the string
 * @c: character to b checked for in the string
 * Return: the string that matches with c
 */

char *_strchr(char *s, char c)
{
	int i;

	for (i = 0; s[i] >= '\0'; i++)
	{
		if (s[i]
		s[i] = c;
	return (s);
}
