#include <stdio.h>
/**
* main - Entry
* Return: Always 0
*/
int main(void)
{
	/* Strings Begin */
	char str[] = "\"Programming is like building a multilingual puzzle";
	/* Now print on OUT */
	puts(str);
	return (0);
}
